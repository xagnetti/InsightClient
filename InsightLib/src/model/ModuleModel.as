package model
{
    import flex.utils.spark.IClassNamable;

    [Bindable]
    public class ModuleModel implements IClassNamable
    {
        public var name : String;
        public var className : String;
    }
}
